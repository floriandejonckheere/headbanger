#import "../../types/User.graphql"
#import "../../types/Credential.graphql"

mutation signup ($email: String!, $password: String!) {
  userSignUp (email: $email, password: $password, passwordConfirmation: $password) {
    authenticatable {
      ...User
    }

    credentials {
      ...Credential
    }
  }
}
